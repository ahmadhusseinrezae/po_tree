{erl_opts, [debug_info]}.
{deps, []}.

{plugins, [rebar3_proper]}.

{profiles,
    [{test, [
        {deps, [{proper, "1.2.0"}, recon]}
    ]}
    ]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [range_tree]}
]}.

